import React from "react";

const EditRadioBtn = (props: any) => {
  const { id } = props;
  // const editableField = droppedContainer.filter((item: any) => item?.id === id);
  // console.log(editableField);
  return (
    <>
      <div>EditRadioBtn</div>
    </>
  );
};

export default EditRadioBtn;
